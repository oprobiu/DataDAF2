name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  validate:
    uses: oprobiu/AFAS/.github/workflows/ci-dataset.yml@main
    with:
      deck-configs: 'data/ro/deck.json,data/en/deck.json'
      tools-version: main
