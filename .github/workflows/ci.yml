name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  validate:
    uses: oprobiu/AFAS/.github/workflows/ci-dataset.yml@v0.3.0
    with:
      deck-configs: 'data/ro/deck.json,data/en/deck.json'
      tools-version: v0.3.0
