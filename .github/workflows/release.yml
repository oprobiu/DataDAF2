name: Release
on:
  push:
    tags: ['v*']
jobs:
  build:
    uses: oprobiu/AFAS/.github/workflows/build-release.yml@v0.2.1
    with:
      tools-version: v0.2.1
    permissions:
      contents: write
