name: Release
on:
  push:
    tags: ['v*']
jobs:
  build:
    uses: oprobiu/AFAS/.github/workflows/build-release.yml@main
    with:
      deck-configs: 'data/ro/deck.json,data/en/deck.json'
      tools-version: main
    permissions:
      contents: write
